import styled from 'styled-components';

export const MemberJoinBox = styled.section`
    max-width: 400px;
    margin: 40px auto 0;
    padding: 20px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    border-radius: 8px;
`;
